package br.com.conexaoautolib.storage;

/**
 * Token storage abstraction and implementations.
 * 
 * <p>This package provides interfaces and implementations for storing
 * OAuth2 tokens, including in-memory storage for caching and 
 * performance optimization. Token storage abstracts the caching
 * strategy from the main business logic.</p>
 * 
 * @since 1.0.0
 */
public class StoragePackagePlaceholder {
    // Placeholder to ensure package exists in JAR
}